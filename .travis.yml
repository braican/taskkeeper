language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules

before_install:
  - nvm install

install:
  - npm install
  - npm run build

deploy:
  provider: script
  skip_cleanup: true
  script: rsync -r --delete-after --quiet $TRAVIS_BUILD_DIR/build braican@69.164.211.156:/var/www/tk2


on:
  branch: setup